from django.db import models

# Create your models here.

# class Diploma(models.Model):
	
# 	#precoprod = models.DecimalField( max_digits=8, decimal_places=2, null=False, blank=False )
#     id_curso = models.ForeignKey(Curso, on_delete=models.CASCADE)
# 	id_instituicao = models.ForeignKey(Instituicao, on_delete=models.CASCADE)

#     dados_dirigente = models.ForeignKey(Dirigente, on_delete=models.CASCADE)


#     informacao_instituicao = models.CharField(max_length=200, null=True)


# 	def __str__(self):
# 		return f'{self.id_diploma}'